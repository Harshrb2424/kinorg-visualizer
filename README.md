# kinorg-visualizer
A local-first, serverless hierarchy visualizer designed for mapping complex family lineages and corporate organizational charts. Built with React and React Flow, it features rich profile management (images, bio, work, education) with 100% data privacy via client-side JSON storage.
